[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "devanagari-fonts"
version = "0.2.0"
description = "Devanagari font discovery, preview paths, and on-demand font installation."
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [
  { name = "Nishant Mishra", email = "nisantamisra00@gmail.com" }
]
keywords = ["devanagari", "fonts", "typography", "hindi", "marathi", "sanskrit"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Multimedia :: Graphics",
  "Topic :: Text Processing :: Fonts",
  "Typing :: Typed",
]

[project.optional-dependencies]
dev = [
  "build>=1.2",
  "pytest>=8.0",
  "twine>=5.0",
]

[project.urls]
Homepage = "https://github.com/nishantaMishra/devanagari-fonts"
Repository = "https://github.com/nishantaMishra/devanagari-fonts"
Issues = "https://github.com/nishantaMishra/devanagari-fonts/issues"

[project.scripts]
devanagari-fonts = "devanagari_fonts.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
devanagari_fonts = ["fonts/Shobhika-1.05/*", "registry.json", "py.typed"]
